import { TemoDirective } from './temo.directive';

describe('TemoDirective', () => {
  it('should create an instance', () => {
    const directive = new TemoDirective();
    expect(directive).toBeTruthy();
  });
});
